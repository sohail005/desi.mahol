{
  "rules": {
    "songsAudio": {
      "$songId": {
        ".read": true,
        ".write": "auth != null && auth.token.admin === true"
      }
    },
    "songsThumbnails": {
      "$songId": {
        ".read": true,
        ".write": "auth != null && auth.token.admin === true"
      }
    }
  }
}
